syntax = "proto3";

package popup;

service PopupService {
  rpc GetPopupMessage(PopupRequest) returns (PopupReply);
}

message PopupRequest {
  string session_id = 1;
}

message PopupReply {
  string message = 1;
}
